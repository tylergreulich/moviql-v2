# import { Movie } from '../movie/schema.graphql'

type Actor {
  _id: ID!
  name: String!
}

type Mutation {
  addActor(_id: ID!, name: String!): Actor
  deleteActor(movieId: ID!, actorId: ID!): Actor
}
